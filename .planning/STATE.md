# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-26)

**Core value:** A beautiful, legible 3D scene that lets you grasp the state of your Docker environment at a glance — what's alive, what's hot, what's connected to what.
**Current focus:** Phase 5 (Theming, Config & Robustness) IN PROGRESS. 05-01 COMPLETE (TOML config substrate — THEME-06 v1 loader landed end-to-end). 05-02 COMPLETE (three named palette presets + by_name dispatcher landed in commit `107c917`; THEME-02 functionally satisfied — runtime switching lands in 05-04). Next: 05-03 (omarchy-derived 4th palette — depends only on 05-02's dispatcher) OR 05-04 (runtime palette switching — THEME-04, depends on both 05-01 AppConfig.palette and 05-02's presets).

## Current Position

Phase: 5 of 5 (Theming, Config & Robustness) — IN PROGRESS
Plan: 2 of 6 complete (05-01 ✅ + 05-02 ✅). Commit `107c917` is the clean 05-02 landing (src/theme/mod.rs +204 lines, 8 new tests). Commit `88791ef` (also titled 05-02) accidentally captured 05-01's `src/config/mod.rs` due to an auto-watcher staging-area reshuffle; its content is correct for 05-01 even though the title says 05-02 — that mis-attribution is a permanent commit-message artifact, the on-disk state is fully consistent.
Status: 05-02 COMPLETE — three named palette presets (notion-soft / cyberpunk-neon / terminal-green) + kebab+snake-case-tolerant by_name dispatcher + by_name_or_default typo-safe fallback. Palette::default() now delegates to notion_soft (single source of truth — zero-regression for every Phase 1–4 snapshot). 8 new unit tests pin: notion_soft legacy-RGB exactness, default == notion_soft, distinct running + glow per preset, by_name dispatch, kebab/snake case tolerance, by_name_or_default fallback, truecolor invariant on every field of every preset. Tests 277 → 285 (+8 in theme::). cargo build --release clean. THEME-01 invariant intact (`grep -rn 'Color::Rgb' src/ | grep -v 'src/theme/'` set did NOT grow). Tuning of preset RGBs deferred to 05-04 visual verify.

Previous: 05-01 COMPLETE — TOML config substrate (THEME-06 v1) shipped. AppConfig serde struct with 5 Phase-5 fields (palette, hud_visible, auto_degrade, force_mode, degraded_fps_cap). load_or_default + load_or_default_with_cli with #[serde(default, deny_unknown_fields)] for graceful per-field defaults + typo trap. ConfigError { Io, Parse } with Display that includes path + actionable hint. default_config_path() via dirs::config_dir resolves $XDG_CONFIG_HOME/3dd/config.toml. main.rs loads config AFTER install_hooks + --dump-* arms but BEFORE connect_and_probe + Tui::enter (Pitfall 9 symmetry with the docker probe — clean-terminal eprintln + exit(1) on Err). `let _ = config;` stub in main.rs that 05-04 removes when wiring AppConfig into App::with_docker / run_kitty signatures. 9 unit tests pin every contract. Pitfall 9 verified end-to-end on the real binary: malformed TOML and explicit-override missing-file both eprintln + exit 1 with no raw mode entered. 276 → 285 tests (+9 in config::). cargo build --release + cargo clippy --all-targets -- -D warnings + cargo test all clean. Bollard isolation intact.

KNOWN ISSUE (RESOLVED — DOES NOT BLOCK): external auto-watcher committed Task-2 work under commit `88791ef` with message body "feat(05-02): three named palette presets + by_name dispatcher" — but the actual diff is `src/config/mod.rs` (412 lines = AppConfig loader). The real 05-02 palette work now lands cleanly at commit `107c917` (executed 2026-05-30 by a fresh /gsd:execute-plan invocation against 05-02-PLAN.md). On-disk state is fully consistent; the only artifact is the misleading title on `88791ef`. Lesson for future executors: when a directory has an active auto-sync watcher, `git diff --cached <file>` immediately before every commit is mandatory.

Phase 4 COMPLETE. 04-06b landed the popup + off-thread spawn closing CAM-05: Effect::SpawnInspect (returned by 04-03's apply_input_action on Enter) is interpreted by App via tokio::spawn(fetch_detail) and by run_kitty via Handle::current().spawn (kitty is sync). On Ok the snap returns via the existing mpsc::UnboundedSender<DockerMsg> as DockerMsg::Inspected; the non-blocking drain routes it into selection.pending_detail and clears inspect_in_flight. Render loop NEVER blocks (Pitfall 8 closed). New src/ui/detail_panel.rs draws Clear + Block + Paragraph (braille, ~60% centered); kitty mirrors via unicode box-drawing chars printed AFTER emit_kitty so the image protocol doesn't overwrite the popup cells. Live block I/O R/W bytes update every frame (popup re-reads LiveWorld::last_sample(selected_id), ~1Hz with Stat cadence; visually verified W 28.3 MB → 34.9 MB across 1.5s). Escape closes (detail_open=false, pending_detail kept cached for instant reopen); Enter on already-open popup re-fires fetch_detail. main.rs cloned the Docker handle into App::with_docker(d, rx) and run_kitty(d, rx); single mpsc channel + single producer task with .abort() on shutdown unchanged. Real-daemon visual verify against `dd3-test-busy1` (alpine + dd write loop) created mid-session — popup showed every required field (name/status/health/image/started/restarts/network/ports/mounts/live blkio); 9 frames captured; all 5 Phase 4 success criteria observably TRUE simultaneously. 250 → 268 tests (+18). cargo clippy --all-targets -- -D warnings clean; cargo build --release clean; bollard isolation intact (Docker handle is the only bollard type that escapes src/docker/ via crate::docker::Docker re-export). 04-06a NOTES PRESERVED for reference: 04-06a lands the DATA-PLUMBING HALF of CAM-05 (Detail Panel) — pure bollard-free substrate that 04-06b will spawn + render against. ZERO visible UI changes. New StatSample.blkio_r_bytes + blkio_w_bytes (u64) summed from blkio_stats.io_service_bytes_recursive 'Read' / 'Write' rows in sample_from_response (case-insensitive on op; Sync/Async/Total ignored to avoid double-count; warming_up forces both to 0). New docker::inspect::DetailSnapshot bollard-free shape (id/name/status/health/started_at_iso/restart_count/restart_policy/image_human/image_digest/network_mode/networks/ports/mounts) covering every field RESEARCH listed EXCEPT SizeRw/SizeRootFs (Pitfall G locked: size(false) on all 3 inspect_container calls; SizeRw/SizeRootFs deferred to v2 DATA-01). New HealthSummary enum (None/Starting/Healthy/Unhealthy{failing_streak, last_output}) — older Moby with missing failing_streak defaults to 0; unhealthy picks LAST log entry's output (freshest probe). New MountSummary struct (kind/source/destination/rw) — rw=None defaults to true (Docker's default mount mode). New fetch_detail(&Docker, &str) -> Result<DetailSnapshot, ProbeError> — async, off-thread-spawnable (04-06b's tokio::spawn target); reuses 03-02's 4-variant ProbeError via new pub(crate) classify_default_path helper in connect.rs (classify itself promoted from private). New DockerMsg::Inspected(DetailSnapshot) variant routed through LiveWorld::handle_inspected -> last_inspect: HashMap<String, DetailSnapshot> cache; pure side data (returns false — no World rebuild, same as Stat). New LiveEntry.last_sample: Option<StatSample> tracking the newest NON-WARMING sample (warming-up explicitly NOT cached); written in handle_stat BEFORE the target_load dedup so same-load Stats still refresh blkio counters. New LiveWorld::last_inspect(id) + last_sample(id) accessors expose the cached payload to 04-06b without re-fetching. New Selection.pending_detail: Option<DetailSnapshot> + Selection.inspect_in_flight: bool slots — next/prev/reconcile clear BOTH so the popup doesn't show stale data + spawn-fetch flag doesn't wedge on disappeared selections. handle_removed drops last_inspect[id] so re-created containers with the same id start fresh. 250/250 tests pass (+24 new: 4 docker::streams blkio + 7 docker::inspect Health/Mount/Network mappings + 7 world::live Inspected cache + last_sample + 6 world::selection pending_detail + in_flight). cargo clippy --all-targets -D warnings clean; release build clean; bollard isolation intact (no bollard imports outside src/docker/).
Last activity: 2026-05-30 — Completed 05-02 (three named palette presets + by_name dispatcher; THEME-02 functionally satisfied). Single corrective atomic commit `107c917` by OnixDebian (src/theme/mod.rs +204 lines, 8 new tests). Tests 277 → 285 (+8 in theme::). SUMMARY at .planning/phases/05-theming-config/05-02-SUMMARY.md. cargo build --release clean.

Earlier 2026-05-30 — Completed 05-01 (TOML config substrate, THEME-06 v1). Three task commits all by OnixDebian, no Claude mentions: `373a923` (chore: serde+toml+dirs deps), `88791ef` (feat: AppConfig + loader + 9 tests — labeled 05-02 by auto-watcher but diff is config/mod.rs), `7758aa6` (feat: wire config load in main.rs). SUMMARY at .planning/phases/05-theming-config/05-01-SUMMARY.md. Tests 276 → 285 (+9 in config::). cargo build --release + clippy --all-targets -D warnings + cargo test all clean. Bollard isolation intact. Pitfall 9 contract verified end-to-end on the real binary (malformed config and missing --config <path> both eprintln + exit 1 with no raw mode entered).

Previous: 2026-05-29 — Completed 04-06b (detail popup + final Phase 4 gate): three atomic commits (12050ae Effect::SpawnInspect wired to off-thread fetch_detail, f869c1d braille detail-panel popup via Clear+Block+Paragraph, 6dda97d kitty popup via unicode box-drawing chars over image). Final visual verify against real daemon captured 9 frames demonstrating all 5 Phase 4 success criteria simultaneously TRUE. Phase 4 COMPLETE.

## Phase 2 Notes (for Phase 3 planning)

- **Architecture:** App owns a single `World` (src/world/: entity/layout/scene/mod). `synthetic_scene()` → 30 boxes / 3 network-groups, deterministic. Both renderers consume `&[Entity]` + `SceneBounds`; `Camera::frame_scene(&world)` binary-searches distance to projected-AABB fill (FRAME_TARGET_FILL=0.92, binding = horizontal axis at cell_aspect 2). When real Docker data lands (Phase 3), it replaces synthetic_scene() output — keep the same World/Entity shape.
- **Occlusion:** braille = single cross-box painter's sort (face-centroid); kitty = per-pixel z-buffer. Boxes never physically intersect (SLOT_SPACING 2.6), so painter's sort is safe for convex boxes.
- **DEVIATION / RECONCILE (CAM-01):** roadmap criterion #4 was "autopilot ORBIT camera". Human overrode it live → shipped PER-BOX self-spin (each box rotates about its own Y at SPIN_RATE 0.525) + STATIC scene-framed camera (YAW_RATE=0). Motion is framerate-independent (on logic tick). REVISIT in Phase 4 when manual explore (CAM-02/03) lands — decide whether orbit returns or per-box-spin stays.
- **Roadmap deviation carried from Phase 1:** kitty backend front-runs part of Phase 5 ROB-02 (capability/degrade). Still open.

Progress: ██░░░░░░░░░░░░░░ ~33% of Phase 5 (2/6 complete: 05-01 ✅ + 05-02 ✅). Cumulative across project: Phase 1: 5/5, Phase 2: 4/4, Phase 3: 4/4, Phase 4: 7/7, Phase 5: 2/6 → 22/26 plans (~85%).

## Performance Metrics

**Velocity:**
- Total plans completed: 22 (5 Phase 1 + 4 Phase 2 + 4 Phase 3 + 7 Phase 4 + 2 Phase 5)
- Average duration: ~8 min
- Total execution time: ~4 hours

**By Phase:**

| Phase | Plans | Total | Avg/Plan |
|-------|-------|-------|----------|
| 1 (Render Core) | 5/5 ✅ | ~27 min | ~5 min |
| 2 (Scene) | 4/4 ✅ | ~14 min | ~3.5 min |
| 3 (Docker) | 4/4 ✅ | ~12 min | ~3 min |
| 4 (Animation) | 7/7 ✅ | ~280 min | ~40 min |
| 5 (Theming) | 2/6 🟡 | ~8 min | ~4 min |

**Recent Trend:**
- Last 7 plans: 04-03 (~30 min incl. self-verified visual checkpoint with 12 captured frames), 04-04 (~90 min incl. self-verified visual checkpoint with 11+ captured frames and 2 visual-iteration tuning cycles for port glow size + color), 04-05 (~75 min incl. self-verified visual checkpoint with 9+ frames and 1 visual-iteration tuning cycle for the 12-stack cap), 04-06a (~20 min — pure data plumbing, no visual checkpoint), 04-06b (~60 min incl. real-daemon detail-popup visual gate), 05-01 (~5 min — autonomous TOML loader; binary-level Pitfall 9 verify done end-to-end since the binary was buildable), 05-02 (~3 min — autonomous pure-fn additions; needed one corrective commit due to auto-watcher staging-area reshuffle but underlying work was a single Edit).
- Trend: autonomous: true substrate plans (05-01 + 05-02) continue to ship in single-digit minutes. 05-02 hit a real workflow hazard: when an external auto-sync watcher races the git index between `Read` and `git add`, content can get swapped. Lesson: always `git diff --cached <file>` immediately before every commit when an auto-watcher is active. The next Phase 5 plans (05-04 wire config to App, 05-05 ROB-02 degrade) will involve visual checkpoints.

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- Roadmap: render-first build order — validate terminal-3D legibility (Phase 1) before any Docker work
- Roadmap: THEME-01 palette abstraction pulled into Phase 1 (avoid hardcoded-color rework)
- Roadmap: layout algorithm in Phase 2 designed network-aware up front, though networks visualize in Phase 4
- 01-01: Cargo package/binary named `dd3` (Rust crate names can't start with a digit); repo/project stays "3dd"
- 01-01: Added `futures` 0.3 for `StreamExt::next` on crossterm `EventStream`; crossterm uses `event-stream` feature
- 01-01: Frame cadence defaults — render 30 FPS (`tui::RENDER_FPS`), logic tick 60Hz (`tui::TICK_HZ`)
- 01-01: Terminal restore extracted to free `tui::restore()` reused by `Tui::exit`, `Drop`, and the panic hook
- 01-04: `render3d::ViewParams { eye, target, up, fov }` is the SOLE camera input — render3d has no `Camera` type dependency (plan 05's Camera builds a ViewParams and feeds it in)
- 01-04: `ViewParams.fov` overrides `RenderConfig.fov` (camera owns the lens; config keeps near/far/cell_aspect)
- 01-04: Occlusion via painter's sort (farthest-first) + back-face cull — NO per-pixel z-buffer (sufficient for convex boxes, PITFALLS #4)
- 01-04: Framebuffer `(w,h)` is braille SUB-PIXEL resolution (2*cells_w, 4*cells_h); `lit_pixels()` is the plan-05 blit feed
- 01-04: Shading = Lambert orientation (theme::dim, floor 0.35) × distance fog (palette.fog toward background, floor 0.45); base color = palette.status_color(Running), zero inline RGB in render3d
- 01-05: Camera→ViewParams producer side closes the 05→04 decoupling (render3d never imports Camera, no cycle); autopilot step(dt) on the logic tick (framerate-independent)
- 01-05: Frustum-safe orbit — DEFAULT_RADIUS 6.0, DEFAULT_FOV 60° keep all 8 cube vertices in-frustum across the full yaw×pitch orbit (~21.7° margin); pinned by orbit_keeps_all_vertices_in_frustum with 0.12 NDC margin
- 01-05 (human-verify tuning, APPROVED): brightened running/glow indigo #5B5BD6→#8A8AF0; raised MIN_LAMBERT 0.35→0.62 and FOG_MIN 0.45→0.7; PITCH_BIAS ~15° + PITCH_AMPLITUDE ~33° to reveal top/bottom faces; cell_aspect 2.0 (cube reads cubic)
- 01-05: Final yaw rate ~30°/s (YAW_RATE 0.525, ~12s/revolution) — bumped 1.5x from ~20°/s on human request
- 01-05: y-flip lives once in NDC→screen projection (01-03); SceneShape blit does NOT re-invert — cube confirmed right-side-up

**Phase 3 (Docker Data):**
- 03-01: PURE normalizer — bollard-free. `src/docker/stats.rs` owns `RawCpu`/`RawMem` (plain input structs 03-03 maps `ContainerStatsResponse` onto) + `StatSample` (cpu_pct/mem_used/mem_limit/mem_fraction/load/warming_up) + `normalize()`. CPU%-delta formula per PITFALLS Pitfall 1 with EVERY guard pinned by 13 unit tests on synthetic before/after samples.
- 03-01: `load = max(cpu_norm, mem_fraction)` in `[0,1]` — box grows for whichever resource it is hot on; `cpu_pct` + `mem_fraction` also exposed for future HUD.
- 03-01: warming-up detection = `prev_cpu.is_none()` OR prev counters both zero (covers bollard's first-frame `precpu_stats` shape); load forced to 0.0.
- 03-01: counter regression (`cur < prev`, e.g. daemon/container restart) also yields 0.0 cpu_pct — added as an explicit guard beyond the plan's list (auto-fix Rule 1).
- 03-01: f64 for delta math (counters are large u64), f32 only on user-facing fields; final-scrub coerces any non-finite slip to 0.0.
- 03-01: `src/docker/mod.rs` declares `pub mod stats;` live + MARKED commented stubs for `domain` / `connect` / `streams` — 03-02/03-03 each uncomment exactly one line when their file lands (no mod.rs conflict). Re-exports `normalize, RawCpu, RawMem, StatSample` for `crate::docker::normalize` call sites.
- 03-01: 64 → 77 tests; `cargo clippy --tests -- -D warnings` clean; stats.rs has zero `use bollard`.
- 03-02: bollard 0.21 wired (`Cargo.toml`), resolves with tokio 1.47, no conflict. bollard 0.21 type/module paths PINNED in 03-02-SUMMARY for 03-03: `bollard::Docker`, `Docker::connect_with_local_defaults` (sync), `Docker::version`/`ping` (async, in `bollard::system`), `bollard::models::ContainerStatsResponse` + `ContainerSummary` + `ContainerState` (re-exports of `bollard_stubs::models`), `bollard::query_parameters::StatsOptions[Builder]` (re-export of stubs), `Docker::stats(name, Option<StatsOptions>) -> impl Stream<Item = Result<ContainerStatsResponse, Error>>`.
- 03-02: `src/docker/domain.rs` (NEW) is the bollard isolation boundary (ARCHITECTURE Anti-Pattern 4). `ContainerSnapshot { id, name, status: theme::Status, group_key }` — bollard-free downstream shape. `map_status(state: &str, oom: Option<bool>, exit_code: Option<i64>) -> theme::Status` total mapping (Docker `dead`/exited-OOM/exited-nonzero → Crashed; exited(0)/created/removing/stopping/unknown → Stopped safe default; running/paused/restarting → direct). `from_bollard_summary(&bollard::models::ContainerSummary) -> ContainerSnapshot` is the SOLE function importing bollard container types (strips Docker's leading '/' on names; primary network = sorted-keys-first, "none" default).
- 03-02: `src/docker/connect.rs` (NEW) — `connect_and_probe()` async, returns `Result<Docker, ProbeError>`. Builds via `connect_with_local_defaults`, probes with `version()` (full handshake/parse, single request). `ProbeError` 4 variants: SocketMissing (path + install hint) / PermissionDenied (path + "docker group" hint) / DaemonDown (path + "systemctl start docker" hint + raw detail) / Other (raw detail). Classifier matches bollard `SocketNotFoundError`/`IOError` directly + walks `Error::source()` chain looking for inner `io::Error` (handles bollard 0.21's hyper/hyper-util legacy wrapping); unknown io kinds and unclassified bollard errors fall back to DaemonDown with raw detail — NEVER panics, ALWAYS yields a non-empty actionable message (ROB-01 invariant pinned by test). `connect_and_probe` performs ZERO terminal I/O — caller (03-04) prints + exits on `Err` BEFORE `Tui::enter` (Pitfall 9).
- 03-02: `mod.rs` uncomments `pub mod domain;` + `pub mod connect;` (the marked lines 03-01 left); re-exports `ContainerSnapshot/map_status/from_bollard_summary/connect_and_probe/ProbeError` on `crate::docker`. `mod docker;` is in main.rs (03-01's wiring); 03-02 did not touch main.rs.
- 03-02: 25 new tests (11 domain + 14 connect); 77 → 102 tests. `cargo clippy --all-targets -- -D warnings` clean. `bollard::*` imports CONFINED to `src/docker/connect.rs` (`use bollard::Docker;` + `use bollard::errors::Error as B;` in classifier) and the fully-qualified `bollard::models::ContainerSummary` in the single `from_bollard_summary` fn in `src/docker/domain.rs`.
- 03-03: PRODUCER/CONSUMER split via `DockerMsg` enum (Added / Removed / StatusChanged(id, theme::Status) / Stat(id, StatSample)) — bollard-free message bus over `tokio::sync::mpsc::UnboundedSender`. `src/world/live.rs` (NEW) `LiveWorld::apply(msg) -> Option<World>` reconciles into the EXISTING `World { entities, bounds }` shape; reuses `layout()` + `load_to_half_extent()` + `SceneBounds::from_entities` verbatim (no signature changes to entity/layout/scene).
- 03-03: STABLE PER-CONTAINER SLOT (CONT-05 anti-teleport) via PER-GROUP slot vectors. `groups: HashMap<String, u16>` insertion-ordered; `group_slots: Vec<Vec<Option<String>>>` indexed by group id; `id_to_addr: HashMap<String, (u16, usize)>` reverse index. On `Removed` only the SAME-group slot is nulled — different-group containers cannot consume same-group holes, so same-group neighbours never shift. The first pass tried a global slot space and the anti-teleport test caught the bug; per-group structure replaced it (auto-fix Rule 1).
- 03-03: `Entity.id = (group_id as u32) * (1<<16) + index_in_group` — stable per id, unique while alive, fits u32, no hashing. Documented in `src/world/live.rs` module header; tests use the `eid(group, idx)` helper.
- 03-03: WARMING-UP samples are no-ops in LiveWorld (don't overwrite a real previous load with the zero from a fresh warming-up frame); load==same is also a no-op (no World rebuild churn). Unknown-id messages (race-window between events and stats) are silent no-ops, never panics.
- 03-03: `src/docker/streams.rs` (NEW) `spawn_docker_tasks(docker, tx) -> JoinHandle<()>` orchestrator: (1) `list_containers(all=true)` seed via `from_bollard_summary` -> `Added` + spawn stats task per Running; (2) `events()` loop reconciles via container actions (create/start/pause/unpause/restart/die/oom/destroy); (3) per-container `stats(id, stream:true)` task feeds bollard `ContainerStatsResponse` -> `sample_from_response()` -> 03-01's `normalize()` -> `Stat`. NEVER `.unwrap()`/`.expect()` a stream item; stream end/error ends task cleanly (events::die/destroy handles entity removal).
- 03-03: CANCELLATION via `HashMap<String, JoinHandle<()>>` owned by the orchestrator task (no Mutex — handle never escapes). die/destroy `.abort()`s the right stats task; final cleanup drains the map. `tokio-util::CancellationToken` REJECTED for this — JoinHandle::abort is sufficient and saves a dep. Idempotent `spawn_stats_for_id_if_absent` checks `JoinHandle::is_finished()` so duplicate start/unpause events don't pile up duplicate streams.
- 03-03: `sample_from_response(&ContainerStatsResponse) -> StatSample` factored as a pure helper (no daemon needed for unit tests). cgroup v2 `inactive_file` -> cgroup v1 `cache` -> 0 picker per-sample. `online_cpus: Option<u32>` widened to u64 to match `RawCpu`.
- 03-03: `src/docker/mod.rs` uncommented `pub mod streams;` (the marked line 03-01 left) + `pub use streams::spawn_docker_tasks;`. `src/world/mod.rs` added `pub mod live;` + `pub use live::{DockerMsg, LiveWorld};`.
- 03-03: 14 new tests (9 live + 5 streams); 102 → 116 tests. `cargo clippy --all-targets -- -D warnings` clean. `bollard::*` imports STILL CONFINED to `connect.rs` + `domain.rs` (single fn) + new `streams.rs` (the mapping helpers `raw_cpu_from` / `raw_mem_from` + the bollard event/stats types in the orchestrator).
- For 03-03 (KNOWN, for 03-04 to consider): `create` event seeds `group_key: "none"` because bollard `EventMessage.actor.attributes` doesn't carry network attachments — Phase 4 may add a targeted `inspect_container` on `start` to backfill. The `events()` stream subscribes with NO filter (client-side filtering on `EventMessageTypeEnum::CONTAINER`); Phase 4 can switch to a server-side `filters({"type": ["container"]})` if wire chatter becomes a problem. `spawn_docker_tasks` returns a `JoinHandle<()>` 03-04 should `.abort()` on shutdown.
- 03-04: PRE-TUI PROBE in `main.rs` (Pitfall 9 / ROB-01 closed end-to-end). `docker::connect_and_probe().await` runs BEFORE `Tui::enter` (braille) AND BEFORE `run_kitty`'s `enable_raw_mode` (kitty) — both backends share ONE gate. On `Err(e)`: `eprintln!("{e}")` + `std::process::exit(1)` lands the `ProbeError`'s actionable message on a CLEAN terminal (verified against `DOCKER_HOST=unix:///tmp/no_such_sock.sock` -> "Docker socket not found … is Docker installed and running?" exit 1, no garble). `--dump-rgba` stays daemon-free (synthetic-only inspection tool; documented inline).
- 03-04: CHANNEL OWNERSHIP in main.rs. Single `mpsc::unbounded_channel::<DockerMsg>()` per process; `spawn_docker_tasks(docker, tx) -> JoinHandle<()>` is held in `main` and `.abort()`-ed after the backend returns (no orphan producer). `rx` is threaded into the chosen backend (`App::with_docker_rx(rx)` for braille, `run_kitty(rx)` for kitty).
- 03-04: BRAILLE App (src/app.rs). `App` gained `docker_rx: Option<UnboundedReceiver<DockerMsg>>` + `live: LiveWorld`. `with_docker_rx(rx)` starts with an EMPTY World (banner-first — criterion #5); `App::new()` preserved for tests / `--dump-rgba` (synthetic-seeded). `drain_docker()` is a non-blocking once-per-outer-loop drain (`while let Ok(msg) = rx.try_recv()`) returning `true` iff entity COUNT changed. `run()` calls `camera.frame_scene(&self.world)` ONLY on count change — pure Stat samples rebuild the World (resize) but never re-frame, avoiding per-second view jitter. 5 new tests pin: empty start, drain rebuilds, empty-channel no-op, Stat-only no count-change, removal-to-empty + finite degenerate bounds.
- 03-04: KITTY run_kitty stays SYNC (UnboundedReceiver::try_recv needs no runtime; producer task already on main's tokio rt). Replaced `synthetic_scene()` seed with `LiveWorld` + empty `World`; same drain + count-change re-frame discipline as braille (`last_count` tracker). Empty world: SKIP `render_rgba` entirely, write centered `ui::EMPTY_BANNER` via cell math (`rows/2`, centered col); `last_was_empty` flag clears the image surface exactly once on the empty<->non-empty edge so the banner doesn't flicker. Status bar gains `boxes: N`. `render_rgba` / `emit_kitty` / 33ms pacing / quit-key handling untouched (git diff src/render3d/ EMPTY).
- 03-04: EMPTY-STATE BANNER single source of truth at `crate::ui::EMPTY_BANNER`. Braille: centered italic `Paragraph` (palette.edge gray) inside the same bordered "scene" block — chrome doesn't change between empty and non-empty. Kitty: centered plain text via cell math.
- 03-04: LIVE VISUAL VERIFY (MEMORY: screenshot-&-verify). Against the real local daemon (13 stopped + 1 running baseline): inside a single 6-second kitty session, status-bar `boxes:` field walked `0 → 14 → 15 → 14` as we `docker run` / `docker rm -f` a test container mid-session. Box COUNT matches `docker ps -a`. Synthetic `--dump-rgba` PNG confirms render path unchanged (indigo/red/gray/amber/cyan all correct).
- 03-04: 116 → 121 tests; `cargo build --release` clean; `cargo clippy --all-targets -- -D warnings` clean. Phase 3 COMPLETE — all five criteria observably TRUE.
- For Phase 4 (carried over): mid-session daemon-loss UX is currently "freeze on last frame" (no panic, no void — hard requirement met); a full "daemon lost" banner with reconnect is Phase 5 ROB-02. `--dump-rgba` stays synthetic-only by design; if a live-data offline dump is ever needed it's a separate small tool.

**Phase 4 (Animation & Interaction):**
- 04-01: BREATHING BOXES (CONT-03) shipped — RESEARCH Pitfall A closed. New `src/world/easing.rs` is std-only and pure: `critically_damped(&mut x, &mut v, target, half_life, dt)` using Holden's EXPONENTIAL closed-form one-step solution `x_next = (d·(1+ωdt) + v·dt)·e^(-ωdt) + target` / `v_next = (v·(1-ωdt) - d·ω²·dt)·e^(-ωdt)`. Truly framerate-independent (pinned by `framerate_independent_within_tolerance` at 0.005 tolerance, dt=0.033 vs dt=0.0033 10× ratio); implicit Euler had a dt-dependent damping bias that failed that pin.
- 04-01: NATURAL FREQUENCY ω = 2·ln(2)/half_life (Holden's `dampingFromHalflife`), NOT the plan's bare `ω = ln(2)/half_life`. The factor of 2 puts the 2% settle threshold at ~4.21·half_life (close to the canonical 4τ rule); bare `ln(2)/hl` gets only 76% at 4τ. The plan's literal Code Example #1 also had a velocity-update typo (`det_v = v + oo * (x - x_target)` missing the dt factor → `hoo`) — switching to the exponential form sidesteps it entirely.
- 04-01: `BREATHING_HALF_LIFE = 0.15s` (NAMED knob in easing.rs). 4·hl ≈ 0.6s to ~97%; 5·hl ≈ 0.75s to ~2%. Inside the 1Hz stat cadence; visible easing without lag.
- 04-01: CADENCE SPLIT in `src/world/live.rs`. `LiveEntry.{target_load, displayed_half, vel_half}` replaces the old `load`. `apply()` is TOPOLOGY (Added/Removed/StatusChanged still rebuild World; Stat returns `None` → no rebuild on Stat samples). `dress(dt, &mut [Entity])` is the per-FRAME size easing pass — walks entries, computes target_half from `(target_load, status)` exactly like the old build_world, calls `critically_damped`, mutates `Entity.half_extents` IN PLACE on the renderer's slice. O(n) `entity_id -> slice_idx` HashMap per call (microseconds at <1000 containers). Empty slice + dt non-finite/≤0 are no-ops.
- 04-01: `handle_status` clears `target_load` on Running ↔ non-Running transitions so dress() switches buckets (load curve vs BASELINE_HALF_NO_LOAD) immediately — without this, a just-Stopped Running box would still ease toward MAX_HALF for the rest of its life (no Stat will ever arrive to override).
- 04-01: `build_world` now reads `entry.displayed_half` directly (not derived per-rebuild from load+status). FIRST world after Added shows the seeded floor (Running→MIN_HALF, non-Running→BASELINE); the next tick begins easing toward the target — that's the visible breath. The renamed test `add_then_dress_sizes_box` pins this with `lw.dress(10.0, ...)` as a fast-forward.
- 04-01: BOTH backends call `live.dress(dt, &mut world.entities)` once per frame from their render loops, using REAL elapsed dt. Braille: `App::on_tick` right after the spin update. Kitty: `run_kitty` main loop right after `spin = ...`, before the empty-vs-render branch.
- 04-01: VISUAL VERIFY done under tmux against `--test` mode (real daemon). 11 captured braille frames at 0.5s and 2s spacings (`/tmp/v401-frame-{1..6}.png`, `/tmp/v401b-frame-{1..5}.png`) show clear sinusoidal size sweep on Running boxes (different phases per box via the `--test` mode's `phase = i * 0.6` offset) and stable BASELINE size on Stopped wireframes. No dribble specks; faces stay flat-shaded; no regression vs Phase 3 baseline.
- 04-01: Tests 121 → 145 (+24: +7 easing.rs, +6 dress contract pins in live.rs, +concurrent 04-02 auto-commits brought ~+11). `cargo build --release` + `cargo clippy --all-targets -- -D warnings` clean.
- For 04-02 (already auto-committed in-tree by host's sync system between Tasks 1 and 2): `feat(04-02): extend ContainerSnapshot with ports + mount_count` (`275ed9d`) and `feat(04-02): docker::inspect module for on-demand inspect_container` (`803f778`). The `DockerMsg::Enriched` variant + a full slot-migrating `handle_enriched` implementation are present in live.rs; my plan-01 work adds a stub that was subsequently filled in. `LiveWorld::snapshot(&str) -> Option<&ContainerSnapshot>` is the bollard-free read handle for the upcoming detail panel (CAM-05).

- 04-02: BOLLARD-FREE EnrichedSnapshot + docker::inspect — Phase 3 carryovers (1)/(2)/(3) closed end-to-end. `EnrichedSnapshot { id, group_key, ports, mount_count, status_override: Option<Status> }` lives in `src/docker/domain.rs` alongside new bollard-free `PortSummary { private, public, proto }` and `PortProto { Tcp, Udp, Sctp }`. `ContainerSnapshot` extended additively with `ports: Vec<PortSummary>` (filled by seed path via `from_bollard_summary` from `ContainerSummary.ports`, sorted by `(private, proto)` for stability) + `mount_count: usize` (filled by enrich path via inspect's `mounts.len()`) + a `Default` impl so test fixtures and the create-event seed stay terse via `..ContainerSnapshot::default()`. Bollard's `bollard::models::PortSummary` is referenced ONLY via fully-qualified path inside the single `from_bollard_summary` fn — no clash with our domain `PortSummary`.
- 04-02: `src/docker/inspect.rs` (NEW, 302 lines + 5 tests) is the bollard isolation point for `inspect_container` calls. Two entry points: `enrich_snapshot_on_start(docker, id) -> Option<EnrichedSnapshot>` backfills group_key + ports + mount_count; `enrich_snapshot_on_seed(docker, id) -> Option<EnrichedSnapshot>` does the same + a `status_override: Some(Crashed)` gate when `map_status(state, oom_killed, exit_code) == Crashed` (i.e. exited-OOM or exited-nonzero). Both explicitly pass `InspectContainerOptionsBuilder::new().size(false).build()` (Pitfall G — size(true) walks the overlay filesystem per call). Helpers `pick_primary_network` (sort networks-map keys, first wins — matches `from_bollard_summary`'s deterministic policy) and `extract_ports` (parses inspect's "PORT/PROTO" keys, drops malformed, sorts by `(private, proto)`).
- 04-02: `DockerMsg::Enriched(EnrichedSnapshot)` routes through `LiveWorld::apply` to `handle_enriched`. Three effects: (1) IN-PLACE update of `entry.snap.ports` + `entry.snap.mount_count`; (2) when `status_override.is_some()`, promote `entry.snap.status` (currently only Stopped → Crashed used); (3) when `enr.group_key != entry.snap.group_key`, MIGRATE the slot — free old group's slot (handle_removed-style; only nulls one slot, neighbors keep their (group, index)) then allocate lowest-free in new group, update `id_to_addr`, refresh `snap.group_key`. Anti-teleport invariant preserved across migrations: OLD-group neighbors don't shift (we only null), NEW-group neighbors don't shift (we fill a hole or append). Unknown id → silent no-op (race window: container removed between inspect dispatch and inspect return).
- 04-02: `LiveWorld::snapshot(id) -> Option<&ContainerSnapshot>` accessor surfaces ports + mount_count to ENT-02 / ENT-03 (04-04 / 04-05) without re-fetching from Docker.
- 04-02: `docker::streams::spawn_docker_tasks` gains two `tokio::spawn` fire-and-forget paths: (a) SEED PASS — for EVERY seeded container (unconditional; bounded by container count once at startup; `enrich_snapshot_on_seed` returns status_override=None for Running, so noop for healthy seeds). (b) `start` EVENT ARM — after the existing `StatusChanged(Running)` + stats-spawn, also spawns `enrich_snapshot_on_start`. Both feed `DockerMsg::Enriched` back through the existing mpsc — events loop NEVER blocks on inspect (Pitfall 8); failure is silent (enrich returns None on bollard error; snapshot already on the wire stays as-is).
- 04-02: SEED-PASS UNCONDITIONAL SPAWN rationale — (a) bounded by container count once at startup; (b) avoids fragile match on bollard's ContainerSummaryStateEnum string form (has shifted across bollard versions); (c) enrich_snapshot_on_seed returns status_override=None for Running, so noop for healthy seeds. Simpler than a state-string match for the same correctness.
- 04-02: BOLLARD ISOLATION INVARIANT INTACT — `grep -rn 'use bollard' src/ | grep -v src/docker/` returns NOTHING. New bollard imports added in `src/docker/inspect.rs` only (`bollard::Docker` + `bollard::query_parameters::InspectContainerOptionsBuilder`); the fully-qualified `bollard::models::ContainerInspectResponse` is used in pick_primary_network / extract_ports signatures without `use`. The pre-existing 4 bollard import sites (`src/docker/{connect,domain,streams}.rs`) are unchanged.
- 04-02: 4 atomic per-task commits — `275ed9d` (Task 1 ContainerSnapshot extension + 4 new tests) / `803f778` (Task 2 docker::inspect module + 5 new tests) / `1c8ec84` (Task 3 Enriched handler + 6 new tests) / `ea25dea` (Task 4 streams.rs spawns). All by OnixDebian; no Claude mentions; conventional-commit format. Tests 145 → 151 (15 docker::domain + 5 docker::inspect + 21 world::live).

- 04-03: MANUAL CAMERA (CAM-02) + Tab SELECTION (CAM-04) + AUTOPILOT GATE (CAM-03) wired end-to-end across both backends. New `src/camera/manual.rs` impls `Camera::nudge_yaw`/`nudge_pitch`/`nudge_zoom` (yaw wraps `rem_euclid(TAU)`, pitch clamps to `±PITCH_LIMIT`, radius clamps to `[1.2, 24.0]`) + the `YAW_STEP=0.10` / `PITCH_STEP=0.07` / `ZOOM_STEP=0.4` / `MIN_RADIUS=1.2` / `MAX_RADIUS=DEFAULT_RADIUS*4=24.0` constants. `Camera` gained `autopilot_active: bool` (default true); `Camera::step` is a no-op when false; `Camera::on_user_input` is the one-way flip — NO auto-revert in v1 (RESEARCH Open Q1; PROJECT.md doesn't ask for it). `PITCH_LIMIT` promoted to `pub(crate)` so `manual.rs` reuses the existing clamp.
- 04-03: NEW `src/world/selection.rs` — `Selection { selected_id: Option<u32>, last_label_cell: Option<(i16,i16)>, pulse_phase: f32, detail_open: bool }` + `next`/`prev` cycle by `Entity.id`-ascending with wrap-around + `reconcile(&world)` to drop a stale selection on Removed (called from drain_docker's count-change branch) + `tick(dt)` advancing pulse_phase (`PULSE_PERIOD = 1.2s`, wraps in `[0,1)`) + `pulse_multiplier(entity_id) -> f32` returning `1.0 + 0.18 * sin(phase * TAU)` for the selected entity, `1.0` for everyone else. 12 unit tests pin cycle/wrap/reconcile/tick/multiplier.
- 04-03: SINGLE DISPATCH SURFACE PRESERVED via `Effect` return shape. `action.rs` extended Action with `NudgeYaw(f32)`, `NudgePitch(f32)`, `NudgeZoom(f32)`, `SelectNext`, `SelectPrev`, `OpenDetail`, `CloseDetail`. NEW `Effect` enum (None / Quit / SpawnInspect(String)) is the OUTPUT shape of the dispatch function — callers pattern-match in 3 arms. `apply_input_action(action, &mut camera, &mut selection, &world, &live) -> Effect` is the ONE function both `App::update` AND `run_kitty`'s event loop call; PURE on (Camera, Selection, World, LiveWorld) — zero bollard, zero tokio, zero spawn. 04-06's `docker::inspect::fetch_detail` spawn plugs into the `Effect::SpawnInspect` match arm in App / run_kitty (where docker handles live), NOT into `apply_input_action`'s signature. This is W10 closure: the single-dispatch-surface invariant holds, tests stay trivial (no runtime), bollard isolation intact (`grep -rn 'use bollard' src/action.rs` returns NOTHING).
- 04-03: KEY MAP (Action::from_key, the single source of truth). Layout-independent arrows + WASD chars on the same arm (Pitfall D); `+/=/PageUp` + `-/_/PageDown` for zoom; Tab/BackTab for selection; Enter for OpenDetail; Esc maps to CloseDetail at `from_key` level — the dispatch surface resolves Esc-quit vs Esc-close by reading `selection.detail_open`; `q` and Ctrl-C always Quit. `KeyEventKind::Press` AND `KeyEventKind::Repeat` treated identically; `Release` is dropped to Action::None (Pitfall E). All key spellings + the Repeat path covered by 10 new tests.
- 04-03: ALL INTERACTIVE ACTIONS FLIP AUTOPILOT OFF. RESEARCH's `is_camera_key` set was just yaw/pitch/zoom; this plan widened it to include SelectNext/SelectPrev/OpenDetail because once the user is Tabbing through containers they're driving the scene. NudgeYaw/Pitch/Zoom + Select* + OpenDetail all call `Camera::on_user_input` first. Only Quit and None (and CloseDetail in close-only mode) skip the flip.
- 04-03: BRIGHTNESS PULSE OVER OUTLINE (chosen per RESEARCH "Selection (CAM-04)" — cheaper, reads at any zoom, no second drawing pass). Both backends compute `pulse_mult = 1.0 + 0.18 * sin(pulse_phase * TAU)` once per entity (constant across its faces / edges) and multiply it into the resolved face / edge RGB then clamp per-channel to `[0, 255]`. `scale_brightness` helper in `src/render3d/raster.rs` (Color::Rgb handling); `scale_brightness_rgb` mirror in `src/kitty.rs` (packed `(u8,u8,u8)` triples). Per-fragment `pulse_mult` lives on `RenderFace` / `RenderEdge` so the cross-box painter's sort doesn't need to re-resolve it per pixel.
- 04-03: CAM-01 RECONCILE (the Phase-2 deviation is now the v1 design). Autopilot mode = static framed camera + per-box self-spin (the "scene's life signal"). Manual mode = user-driven orbit on top; per-box spin CONTINUES (it's the scene's life regardless of camera mode). No auto-revert from manual back to autopilot. The CAM-01 row in REQUIREMENTS.md's Traceability table should move from "Complete (deviation)" to "Reconciled in Phase 4 — autopilot = spin-in-place + static camera, manual = user-driven orbit; deviation accepted as v1 design".
- 04-03: AUTO-REFRAME GATED behind `camera.autopilot_active` in BOTH backends. `App::run` and `run_kitty`'s count-change branch only call `camera.frame_scene[_with_aspect]` when autopilot is still active. A manual-mode user driving the camera is NOT yanked back by every container add/remove.
- 04-03: STATUS BAR MODE INDICATOR in both backends. `mode: auto` flips to `mode: manual` the moment `Camera::on_user_input` fires — visible feedback for the autopilot/manual transition. Both backends also append the `arrows orbit, Tab select, q to quit` help hint so the keybinds are discoverable without docs (Pitfall D mitigation: arrows are layout-independent and explicitly mentioned).
- 04-03: `LiveWorld::id_string_for_entity(entity_id) -> Option<&str>` REVERSE LOOKUP added — the bridge from `Selection::selected_id` (an `Entity.id` flat encoding) to the docker container id string `Effect::SpawnInspect` carries to 04-06's `docker::inspect::fetch_detail`. O(n) linear walk of `id_to_addr` — bounded by container count, microseconds at our scale. Call site is user-driven (one per OpenDetail), not per-frame.
- 04-03: RENDERER SIGNATURE CHANGES — both `render3d::render_scene` (braille) and `kitty::render_rgba` gained `selected_id: Option<u32>` + `selection_pulse_phase: f32` (Copy primitives, closure-safe for ratatui's `Fn` Canvas paint bound). `SceneShape` (braille) carries the same two Copy primitives so the `Canvas::paint` closure stays trivial-bound. Offline dump call sites (`dump_rgba` / `dump_snapshot` / `dump_rgba_mixed`) pass `None, 0.0` — no selection in offline contexts.
- 04-03: VISUAL VERIFY done under tmux against the braille `--test` mode (12 captured frames). `/tmp/v403-baseline.png` confirms `mode: auto` on launch with the canonical 3/4 framing. After `a` + `d×4`, the rack rotates clockwise on yaw AND the status bar flips to `mode: manual` (first 'a' press was the trigger). `w×3` tilts pitch upward; `+×3` pulls the camera close (radius clamp visible at MIN_RADIUS); `-×6` pushes it back. Tab moves the selection brightness pulse to different boxes — `/tmp/v403-pulse{1..4}.png` (captured at ~0.3s spacing without input) sample different phases of the 1.2s sine and the bottom-left wireframe box visibly brightens/dims between captures. Captured a 2s no-input pair confirming `mode: auto` persists (only fps drifted — autopilot does NOT spontaneously flip).
- 04-03: 4 atomic per-task commits — `fb187b2` (Task 1 camera::manual + autopilot_active gate + 5 tests) / `e40ed6a` (Task 2 Selection module + Action variants + Effect dispatch + 27 tests) / `f3f19a7` (Task 3 braille brightness-pulse + mode indicator + 5 App tests) / `7aec550` (Task 4 kitty backend parity — selection + dispatch + pulse + signature extension). All by OnixDebian; no Claude mentions; conventional-commit format with `(04-03)` scope. Tests 151 → 188 (+37: 5 camera::manual + 12 selection + 15 action/Effect + 5 app).

- 04-04: NETWORK FLOOR-PLANES (ENT-01) + PORT GLOW (ENT-02) + SELECTED LABEL (CONT-04) shipped — the biggest visual landing of Phase 4. New `src/render3d/plane.rs` rasterizes a wireframe quad through 4 projected corners + 4 calls to the shared `draw_thick_line` band-rasterizer at `EDGE_HALF_PX_SS = 1.6` SS-pixel thickness; skip-on-any-corner-clip to avoid partial floors flickering at the frustum edge. Both `draw_thick_line` and `EDGE_HALF_PX_SS` promoted to `pub(crate)` so plane.rs reuses cube wireframe edge weight.
- 04-04: SceneExtras<'a> SHARED STRUCT (W7 closure, owner: 04-04) in `src/render3d/scene_extras.rs` — `floors: &[FloorPlane]` + `ports: &PortLookup<'a>` today; 04-05 EXTENDS fields with `cylinders` + `image_stacks` WITHOUT growing the renderer entry-point signatures (only inner code reads new fields). `FloorPlane { center, half_size_xz, color }` + `PortLookup<'a> = HashMap<u32, &'a [PortSummary]>` (borrows live snapshot ports — no clones). Both `render3d::render_scene` (braille) and `kitty::render_rgba` accept `extras: &SceneExtras<'_>` as their single new param — the trailing position so existing args (entities, view, palette, config, spin, selected_id, pulse_phase) stay put.
- 04-04: FLOOR_Y = -(MAX_HALF + 0.2) = -1.4 (NOT the plan's -0.1). Plan's y = -0.1 was wrong — row-0 box position.y = 0 with half_extents.y up to MAX_HALF = 1.2 means a fully-loaded row-0 box spans y ∈ [-1.2, +1.2]; a floor at y = -0.1 would intersect those boxes. -1.4 places the floor cleanly below every box at every load. FLOOR_PAD = 1.0 world units extends floor past slot AABB on each XZ axis.
- 04-04: LiveWorld::group_bounds_xz() → Vec<(group_key, center, half_size_xz)> — one entry per NON-EMPTY group (every-slot-Removed groups dropped: drawing a floor under nobody is just visual noise). Walked in group_id insertion order so the returned Vec order is deterministic. Reuses `layout::layout` to convert slot indices to world XZ.
- 04-04: FLOOR DRAWN BEFORE CUBES in both backends so the cube painter's sort (braille) and z-buffer (kitty) naturally hide the floor where cubes overlap on screen. Floor color = `palette.edge` (muted gray) for unselected groups, `palette.status_color(Status::Running)` (indigo green) for the network the SELECTED entity belongs to — provides "you are on this network" read.
- 04-04: PORT GLOW = palette.glow (bright indigo #8A8AF0), NOT palette.status_color(Running) (visual-verify deviation Rule 1). Original plan said status_color(Running) but that's the SAME color as the Running cube face — the dots vanished into the box. Switched to palette.glow which is distinct from every cube status color. Pinned in code comments.
- 04-04: PORT DOT SIZE = max(0.18 * face_min, 0.10) world units (visual-verify deviation Rule 2). First pass was `0.08 * face_min` = 0.024 world units at MIN_HALF (0.3) boxes — collapsed below the braille AA majority-coverage threshold. Bumped formula + added 0.10 world-unit floor (~2 braille dots at standard framing distance) so MIN_HALF idle boxes still show readable dots, proportional scaling to MAX_HALF.
- 04-04: PORT FACE PICK = dot of spun face normal with (eye - entity_center), tie-break +Z, +X, -Z, -X (deterministic). Vertical faces only — top/bottom never get ports (ports don't make sense on the floor/ceiling). 3×3 UV grid positions at UV = ((col+1)/4, (row+1)/4) for col,row ∈ {0,1,2} = {0.25, 0.5, 0.75} so dots fill the inner half of the face. Cap 9 ports per face (3×3 grid); extras dropped silently.
- 04-04: PORT GLOW EMISSIVE — direct color write (no Lambert, no fog). FACE_EPS = 1e-3 outward offset along the spun face normal so dot z-depth is fractionally smaller than the face it sits on (z-buffer renders it ON TOP rather than embedding).
- 04-04: LABEL PIPELINE OPTION (a) PINNED — `Canvas::paint` takes `Fn` (not `FnMut`), so `&mut Selection` can't cross the closure boundary. `ui/scene::render_scene` does the entire selection-anchor pipeline (project_label_anchor + snap_anchor_with_hysteresis + name lookup + truncate_label) BEFORE constructing the Canvas; the closure captures only an `Option<(i16, i16, String)>` via `move`. Owned `Line::from(name.clone())` inside the closure side-steps Context's invariant `'a` lifetime parameter.
- 04-04: LABEL RENDERS ONE CELL ABOVE BOX TOP — `row.saturating_sub(1)` in cell coords. Braille uses Y-flipped canvas math coords: `cy_math = (viewport.1 - 1) - (row * 4)`. Kitty uses 1-indexed terminal cells via `\x1b[{row};{col}H{name}` with clamps inside the visible cells (above the reserved status bar row).
- 04-04: PER-BACKEND LABEL HYSTERESIS STATE (Selection.last_label_cell for braille, Selection.last_label_cell_kitty for kitty — RESEARCH Open Question #4 closed: per-backend in v1). The two cell systems disagree (braille is dot/2 × dot/4; kitty is pixel/cell_w × pixel/cell_h), so each backend keeps its own cache. `next/prev/reconcile` reset BOTH caches on selection change so the new label snaps to its anchor immediately.
- 04-04: ui::labels — pure helpers (no I/O, fully unit-tested). `project_label_anchor` builds a non-supersampled Projector matching the renderer's cell_aspect to get screen-resolution coords; returns None on frustum-cull. `snap_anchor_with_hysteresis` is the cell-quantize + >=1-cell drift threshold. `truncate_label` UTF-8-safely caps names at `MAX_LABEL_LEN = 24` chars with `…`.
- 04-04: KITTY STALE-LABEL CLEAR — `last_label_print: Option<(col, row, len)>` local in `run_kitty` tracks the previous frame's printed cell range; cleared with `" ".repeat(plen)` at the previous (col, row) BEFORE writing the new label. Avoids streaks when the selection moves or the box spins to a different camera-face position.
- 04-04: dump_snapshot OFFLINE TOOL INJECTS SYNTHETIC ENRICHED PORTS for every other forced-Running container (1-3 fake TCP ports at private+public 8000+p) — so the offline `--dump-snapshot` exercises the ENT-02 port glow path without needing a live daemon with real port data. Uses crate::docker bollard-free re-exports (PortSummary, PortProto, EnrichedSnapshot).
- 04-04: VISUAL VERIFY done autonomously per user authorization (host onix). 11+ captured frames + 3 RGBA snapshots: `/tmp/v404-baseline.png` (braille --test 3 groups visible), `/tmp/v404-pitch-deeper.png` (floor-planes from above clearly visible under each cluster), `/tmp/v404-pitch-up.png` (floor-planes from below — no occlusion artifacts), `/tmp/v404-snap.png` (kitty dump_snapshot 5 floor-planes one per network, clean rectangles), `/tmp/v404-port-zoom4.png` (cropped + zoomed: 3 distinct INDIGO port dots on a green Running box's front face), `/tmp/v404-final-snap.png` (full kitty dump after port-glow tuning, multiple boxes showing port glow at varied counts).
- 04-04: BOLLARD ISOLATION INVARIANT INTACT — `grep -rn 'use bollard' src/ | grep -v src/docker/` returns NOTHING. All new code in render3d, ui, world is bollard-free; the dump_snapshot synthetic Enriched message uses the pre-existing `crate::docker::{PortSummary, PortProto, EnrichedSnapshot}` re-exports.
- 04-04: 5 atomic commits — `22d757f` (Task 1 render3d::plane + SceneExtras + LiveWorld::group_bounds_xz + 7 tests) / `085a842` (Task 2 ui::labels + Selection kitty cell + 10 tests) / `11f63cf` (Task 3 braille integration via SceneExtras + 3 ui::scene tests) / `6723f79` (Task 4 kitty mirror — floor + port-glow + cell-grid label, no tests added beyond updating existing render_rgba calls) / `01f0593` (visual-verify-driven port-glow color + size fix + dump_snapshot synthetic ports). All by OnixDebian; no Claude mentions; conventional-commit format with `(04-04)` scope. Tests 188 → 208 (+20: 4 group_bounds_xz + 1 id_string_for_entity + 2 plane + 10 ui::labels + 3 ui::scene).

- 04-05: VOLUME CYLINDERS (ENT-03) + IMAGE STACKS (ENT-04) shipped — Phase 4 success criterion #5 fully closed. New `src/render3d/cylinder.rs` rasterizes an N-gon prism (DEFAULT_SIDES=8 octagonal) via top + bottom caps fan-triangulated through the shared `fill_triangle` + N side quads through `fill_face`. Side quads sorted by **yaw-to-camera dot** (each side's outward XZ normal dotted with eye-to-centroid; sort ascending so back-facing paint first, front-facing paint last) — Pitfall H closure, no seam flicker on rotation. Caps sorted by squared camera distance (farther cap first). `pub(crate) fill_face` + `pub(crate) fill_triangle` promoted from private in raster.rs so cylinder reuses the same braille edge-function rasterizer + EDGE_EPS seam tolerance.
- 04-05: New `src/render3d/stack.rs` rasterizes an image-layer stack = N short cubes stacked along +Y at `base`. Reuses `unit_cube` topology + back-face cull + `fill_face`. Constants: `LAYER_HALF = 0.5`, `LAYER_GAP = 1.1` (>2*LAYER_HALF so layers never touch), `MAX_LAYERS = 8` (cap so a 30-layer Debian image doesn't tower). Color = `palette.edge` (muted gray, same as floor lines / wireframe cubes).
- 04-05: NEW `src/docker/images.rs` (bollard isolation point for image data). `ImageSnapshot { id, repo_tag, layer_count }` bollard-free downstream shape. `fetch_image_snapshots(&Docker) -> Vec<ImageSnapshot>` best-effort: `list_images(all=false)` + per-image `inspect_image(id)` → `root_fs.layers.len()`. List failure → empty Vec; per-image inspect failure → skip that entry. Runs ONCE on startup (no per-frame Docker calls). NEW `_compile_check_signatures` `#[allow(dead_code)]` stub references every bollard surface (Docker handle, list_images, inspect_image, ListImagesOptionsBuilder, ImageInspect) — B3 closure pin: forces a build error if bollard renames a builder or shifts a return type. Grep-targetable from CI/verify.
- 04-05: NEW `src/world/volume.rs::proxy_volume_height(mount_count) -> f32` (sqrt-compressive: MIN_VOL_H=0.2 → MAX_VOL_H=1.2 over MOUNT_REF=4 mounts; gain 0→1 > gain 3→4; saturates beyond MOUNT_REF). Pitfall F + G CLOSURE — NEVER reads `VolumeUsageData.size` (which is -1 for non-local drivers) and NEVER calls `inspect_container` with size=true (which walks the overlay filesystem per moby docs). Mount count is the only cheap signal we have.
- 04-05: SceneExtras EXTENDED — `Cylinder { center, radius, height, color }` + `ImageStack { base, layer_count }` types added; `SceneExtras { floors, ports, cylinders, image_stacks }` (W7 closure: renderer entry-point signatures `raster_render_scene` / `render_rgba` UNCHANGED, only struct fields grew). All call sites (raster tests, ui/scene.rs, 5 kitty.rs sites) updated to pass empty slices for non-extras paths or built per-frame extras for live paths.
- 04-05: NEW `world::layout` consts: `MAX_RACK_X = GRID_COLS * SLOT_SPACING` (compile-time deterministic upper bound on rack +X extent — W9 closure: decouples image stack region from live entity set), `IMAGE_REGION_X_OFFSET = 4.0`, `IMAGE_STACK_SPACING_Z = 2.6`. `LiveWorld.image_stack_positions(&self) -> Vec<(Vec3, usize, String)>` uses these constants directly — NO `max_rack_x` argument. Synthetic-only (`--dump-rgba`) paths produce identical stack geometry to live paths (pinned by `image_stack_positions_uses_deterministic_max_rack_x` test).
- 04-05: `LiveWorld.images: BTreeMap<id, ImageSnapshot>` — BTreeMap-by-id walk for stable positions across runs. `DockerMsg::ImageAdded(ImageSnapshot)` + `DockerMsg::ImageRemoved(String)` (latter reserved for v2 — Phase 4 doesn't subscribe image events). `handle_image_added` uses `Entry::Occupied/Vacant`: first insert returns `true` (World rebuild); duplicate id refreshes value but returns `false` (no rebuild). Streams.rs seeds via `fetch_image_snapshots(&docker)` AFTER container seed, BEFORE events loop — best-effort, fire-and-forget through the mpsc.
- 04-05: I12 CLOSURE — NO `LiveWorld::mount_count(id)` accessor added. Call sites use the existing `LiveWorld::snapshot(id).map(|s| s.mount_count)` from 04-02 — same shape as port reads for port glow. Avoids accessor sprawl.
- 04-05: BOTH backends render the new primitives via the extended SceneExtras. Braille (`render3d::raster::render_scene`): floors → cubes → ports → CYLINDERS (`rasterize_cylinder`) → IMAGE STACKS (`rasterize_image_stack`) → labels. Kitty (`kitty::render_rgba`): inline z-buffered N-gon prism for cylinders (per-side back-face cull via outward XZ normal; top/bottom caps back-face culled by Y normal) + inline z-buffered short-cubes for image stacks (back-face culled). New per-frame extras builders: `ui::scene::build_volume_cylinders` + `build_image_stacks` (braille); `kitty::build_volume_cylinders_kitty` + `build_image_stacks_kitty` (kitty mirror).
- 04-05: NEW `palette.volume: Color` (muted teal `Rgb(0x50, 0x8C, 0x8C)`) — distinct from running green / glow indigo / status colors. Reads as "data" against the green/gray scene.
- 04-05: VISUAL VERIFY DEVIATION (Rule 1) — A 27-image developer machine stretched the +Z image region across ~70 world units in the first braille capture, overwhelming the container rack. Added `MAX_VISIBLE_IMAGE_STACKS = 12` cap in `image_stack_positions` — matches the plan's pre-existing `notes_and_risks` TODO ("cap at first ~20 images by truncating in image_stack_positions if it shows in practice"). It showed; capped at 12.
- 04-05: VISUAL VERIFY DEVIATION (Rule 2) — `dump_snapshot` now reads `{{.Mounts}}` from `docker ps -a` CLI and feeds mount_count into ContainerSnapshot via Added (comma-separated mount paths counted; faithful proxy for the inspect path's actual mount list). Without this the offline kitty visual gate had zero cylinders. ALSO seeds 5 synthetic `ImageAdded` messages (varied 1-7 layer counts) so the offline path exercises the ENT-04 image stack rendering pipeline (real `list_images` requires bollard which `dump_snapshot` deliberately avoids).
- 04-05: VISUAL VERIFY done autonomously per user authorization (host onix). 9+ captured frames: `/tmp/v405-snap.png` (kitty dump_snapshot before stack cap), `/tmp/v405-b2-baseline.png` (braille after 12-stack cap — rack reads as focal point again), `/tmp/v405-b2-orbit.png` (orbit + zoom shows discrete horizontal layer seams in an image stack with a teal cylinder on a container), `/tmp/v405-b2-pitch.png` (pitched view shows 2 containers with teal cylinders sitting on top, plus an image stack with 5+ visible layer seams), `/tmp/v405-spin-t0/t1/t2.png` (3 spin frames 2s apart — cylinder silhouettes stay clean, no seam flicker, Pitfall H closure intact), `/tmp/v405-final-snap.png` (kitty dump_snapshot after fixes — floor-planes + teal cylinders + image stack column all visible).
- 04-05: BOLLARD ISOLATION INVARIANT INTACT — `grep -rn 'use bollard' src/ | grep -v src/docker/` returns NOTHING. New bollard imports in `src/docker/images.rs` only (`use bollard::Docker;` + `use bollard::query_parameters::ListImagesOptionsBuilder;`). The pre-existing bollard-importing files (`connect.rs`, `domain.rs`, `streams.rs`, `inspect.rs`) are unchanged in their bollard surface area.
- 04-05: 6 atomic commits — `2ba9c85` (Task 1 docker::images + ImageSnapshot + fetch_image_snapshots + _compile_check_signatures + 1 test) / `b9887fb` (Task 2 render3d::cylinder N-gon prism + per-face yaw sort + 4 tests; pub(crate) fill_face/fill_triangle) / `8ce554f` (Task 3 render3d::stack image-layer stack via cube reuse + 4 tests) / `91afedb` (Task 4 LiveWorld images BTreeMap + SceneExtras extension + layout MAX_RACK_X consts + palette.volume + world::volume + 9 new tests; streams.rs image seed) / `eb897d5` (Task 5 braille + kitty wire cylinders + image stacks through extended SceneExtras; build_volume_cylinders + build_image_stacks builders per backend) / `0cea78b` (visual-verify deviations: 12-stack cap + dump_snapshot mount_count + synthetic ImageAdded). All by OnixDebian; no Claude mentions; conventional-commit format with `(04-05)` scope. Tests 208 → 226 (+18: 1 docker::images + 4 render3d::cylinder + 4 render3d::stack + 5 world::volume + 4 world::live image-handling, including W9 closure pin).

- 04-06a: DETAIL PANEL DATA PLUMBING — pure bollard-free substrate for CAM-05. ZERO visible UI changes; the popup + final phase verify land in 04-06b. New `StatSample.blkio_r_bytes` + `StatSample.blkio_w_bytes` (u64 each) summed from `blkio_stats.io_service_bytes_recursive` 'Read' / 'Write' rows in `sample_from_response` (case-insensitive on `op`; Sync/Async/Total ignored to avoid double-count — `Total == Read + Write`; warming_up forces both to 0). RESEARCH "Field-by-Field Source Map" closure: block I/O lives on StatSample (the existing 1Hz wire), NOT on inspect. All existing StatSample struct-literal sites (main.rs --test mode loop, kitty.rs dump_snapshot, app.rs + world::live.rs tests) updated to set both new fields to 0.
- 04-06a: NEW `docker::inspect::DetailSnapshot` bollard-free struct covering every field RESEARCH listed EXCEPT `SizeRw`/`SizeRootFs` (deferred to v2 DATA-01 — Pitfall G locked: all 3 inspect_container call sites pass `size(false)`; no `size(true)` anywhere). Fields: id/name/status/health/started_at_iso/restart_count/restart_policy/image_human/image_digest/network_mode/networks (Vec<(name, ip)> sorted by name)/ports (reuses extract_ports from 04-02)/mounts. NEW `HealthSummary` enum (None / Starting / Healthy / Unhealthy{failing_streak, last_output}) — `#[derive(Default)]` with `#[default] None`; older Moby builds with missing failing_streak default to 0; unhealthy picks the LAST log entry's `output` (freshest probe message). NEW `MountSummary` struct (kind / source / destination / rw) — rw=None defaults to true (Docker's documented default mount mode). NEW `fetch_detail(&Docker, &str) -> Result<DetailSnapshot, ProbeError>` async (off-thread-spawnable; 04-06b will `tokio::spawn` on Enter). Status re-derived through `map_status` so the popup agrees with seed/event path on Crashed-vs-Stopped (Phase 3 carryover (1) consistency).
- 04-06a: `docker::connect::classify` promoted from private `fn` to `pub(crate) fn` so `fetch_detail` reuses 03-02's 4-variant ProbeError policy. New `classify_default_path(&bollard::errors::Error) -> ProbeError` helper hides `effective_socket_path` so fetch_detail can map errors without owning a resolved path string. `fetch_detail` uses `.map_err(|e| classify_default_path(&e))` so the popup's error message routes through ONE error policy across the docker/ module.
- 04-06a: NEW `DockerMsg::Inspected(DetailSnapshot)` variant routes through `LiveWorld::apply` -> `handle_inspected` which caches into `last_inspect: HashMap<String, DetailSnapshot>` and returns `false` — no World rebuild (matches Stat's no-rebuild rule, RESEARCH Pitfall A: pure side data). NEW `LiveWorld::last_inspect(id) -> Option<&DetailSnapshot>` accessor for the popup. NEW `LiveEntry.last_sample: Option<StatSample>` tracks the newest NON-WARMING StatSample (warming-up explicitly NOT cached — contract is "newest non-warming sample"); written in `handle_stat` BEFORE the target_load dedup so a same-load Stat still refreshes blkio counters (blkio bytes are MONOTONIC counters that keep growing even when CPU% is flat). NEW `LiveWorld::last_sample(id) -> Option<&StatSample>` accessor exposes blkio_r/w + cpu_pct + mem_used to the popup without re-fetching.
- 04-06a: `handle_removed` drops `last_inspect[id]` so a re-created container with the same id (rare but possible — `docker rm` + `docker run --name same`) starts fresh; the popup re-spawns fetch_detail rather than show stale data.
- 04-06a: NEW `Selection.pending_detail: Option<DetailSnapshot>` + `Selection.inspect_in_flight: bool` slots ready for 04-06b's spawn handler. `next/prev/reconcile` clear BOTH so the popup never shows stale data for the previous container and 04-06b's spawn-fetch flag doesn't get wedged on a disappeared selection (a fetch_detail dispatched for a since-removed container would otherwise leave `inspect_in_flight` stuck at true). 6 selection tests pin the clear behavior.
- 04-06a: DEVIATIONS (all Rule 1 — auto-fixed bugs prevented BEFORE 04-06b lands, tested):
  - (1) next/prev clear pending_detail + inspect_in_flight even on non-empty new selections — plan only said "ready for 04-06b's spawn handler" but without this clear, the popup would briefly show OLD container's data while 04-06b spawned a fresh fetch.
  - (2) handle_removed drops last_inspect[id] — prevents stale-data leak on container id reuse.
  - (3) handle_stat writes last_sample BEFORE target_load dedup — same-load samples still refresh blkio counters (which are monotonic at the daemon).
- 04-06a: 3 atomic per-task commits — `d22fdef` (Task 1 StatSample.blkio_r/w + streams sum + 4 tests) / `8173eee` (Task 2 fetch_detail + DetailSnapshot + classify_default_path + 7 tests) / `6f2a36d` (Task 3 DockerMsg::Inspected + LiveWorld cache + Selection slots + 13 tests across world::live + world::selection). All by OnixDebian; no Claude mentions; conventional-commit format with `(04-06a)` scope. Tests 226 → 250 (+24: 4 docker::streams blkio + 7 docker::inspect Health/Mount/Network + 7 world::live + 6 world::selection).
- 04-06a: BOLLARD ISOLATION INVARIANT INTACT — `grep -rn 'use bollard' src/ | grep -v src/docker/` returns NOTHING. New bollard usages in `src/docker/inspect.rs` only (existing `use bollard::Docker` + `use bollard::query_parameters::InspectContainerOptionsBuilder` + new fully-qualified `bollard::models::Health` / `MountPoint` / `EndpointSettings` in helpers). No `use bollard::models::*` imports added — references are fully-qualified through `bollard::models::` paths.

**Phase 5 (Theming, Config & Robustness):**
- 05-01: TOML CONFIG SUBSTRATE (THEME-06 v1) shipped — pure foundation for 05-02..06. New `src/config/mod.rs` AppConfig serde struct with EXACTLY the 5 Phase-5 fields: `palette: String` (default "notion-soft"), `hud_visible: bool` (default true), `auto_degrade: bool` (default true), `force_mode: String` (default "auto", values "auto"/"kitty"/"truecolor"/"ascii"), `degraded_fps_cap: u32` (default 15). `#[serde(default, deny_unknown_fields)]` so partial files fill per-field defaults AND typos like `pallete = "x"` are a parse error (typo trap). RenderConfig kept unchanged (no TOML surface in v1 — cell_aspect/fov/near/far stay code-defaulted).
- 05-01: LOADER SEMANTICS pinned by 9 unit tests. `load_or_default(path: Option<&Path>)`: file MISSING → `Ok(Default::default())` (first-run UX must not require a file); file PRESENT + valid → `Ok(parsed)`; PRESENT + malformed → `Err(ConfigError::Parse)` with path + toml::de::Error line/col; PRESENT + I/O error (non-NotFound) → `Err(ConfigError::Io)`. `load_or_default_with_cli(args)`: `--config <PATH>` override is LOUD — explicit override missing file = Err(Io), not silent default. Implicit default-path fallback (no --config) = quiet (Ok(default)).
- 05-01: ConfigError { Io { path, source }, Parse { path, source } } with `Display` that ALWAYS includes the path + actionable hint ("fix the TOML and re-run" / "check file permissions"). Implements `std::error::Error::source()` for downstream introspection.
- 05-01: `default_config_path()` via `dirs::config_dir()` resolves `$XDG_CONFIG_HOME/3dd/config.toml` (or `~/.config/3dd/config.toml`). `dirs 5` chosen over `directories` for the smaller crate footprint (we only need config_dir()).
- 05-01: PITFALL 9 SYMMETRY WITH THE DOCKER PROBE — config load runs AFTER `install_hooks` and AFTER the `--dump-*` early-return arms (offline tools deliberately config-free) but BEFORE `connect_and_probe` and BEFORE `Tui::enter` / `enable_raw_mode`. On Err: `eprintln!("config error: {e}")` + `std::process::exit(1)`. VERIFIED END-TO-END on `./target/release/dd3`: malformed TOML + missing --config <path> both exit 1 with clean actionable terminal output (no raw mode entered, no alt-screen garble). This exceeded the plan's "deferred to 05-04 visual verify" expectation since the binary was already buildable.
- 05-01: `let _ = config;` STUB IN main.rs — 05-01 lands the loader + error path; 05-04 lands the consumer side (threads `config: AppConfig` into App::with_docker / run_kitty signatures, then 05-02/03/05/06 read individual fields). The plan's Task 3 explicitly designed this two-plan split to preserve atomicity.
- 05-01: v1 SCOPE EXPLICITLY EXCLUDES per-color overrides (no `[palette.custom] running = "#aabbcc"`) AND RenderConfig TOML composition. THEME-06 v1 = palettes picked BY NAME from TOML; v2 may add per-color overrides if the named-preset universe proves too narrow.
- 05-01: No tempfile dep — tests build unique paths from `std::process::id() + nanos` and clean up via a `CleanupPath(PathBuf)` Drop guard (panic-safe).
- 05-01: 3 atomic commits — `373a923` (chore: serde 1 + toml 0.8 + dirs 5 deps), `88791ef` (feat: AppConfig + ConfigError + loader + 9 tests — LABELED 05-02 BY AUTO-WATCHER but actual diff is src/config/mod.rs +412 LOC; same labeling quirk as 04-02's "auto-committed in-tree by host's sync system" note), `7758aa6` (feat: wire config load in main.rs before docker probe). All by OnixDebian; no Claude mentions. Tests 276 → 285 (+9 in config::); cargo build --release + cargo clippy --all-targets -- -D warnings + cargo test all clean. Bollard isolation invariant intact (`grep -rn 'use bollard' src/ | grep -v src/docker/` returns nothing — config module is bollard-free as expected).
- 05-01: KEY ACCESSORS / TYPES FOR 05-02..06 — `crate::config::AppConfig` (re-export TBD by 05-04 when consumed), `crate::config::ConfigError`, `crate::config::load_or_default(Option<&Path>)`, `crate::config::load_or_default_with_cli(&[String])`, `crate::config::default_config_path() -> Option<PathBuf>`. The full SUMMARY is at `.planning/phases/05-theming-config/05-01-SUMMARY.md`.

- 05-02 (status note RESOLVED): the clean 05-02 landing arrived as commit `107c917` (2026-05-30). The earlier 88791ef commit (which contained 05-01's config loader diff under a 05-02 title) stays in history unchanged — content is correct for 05-01, the title is the only artifact of the auto-watcher staging reshuffle. The design notes below remain accurate as the shipped 05-02 shape.
- 05-02: NAMED PALETTE PRESETS shipped — THEME-02 functionally satisfied. Three plain-fn constructors on Palette (notion_soft / cyberpunk_neon / terminal_green) + a string-keyed dispatcher (by_name / by_name_or_default). NO trait magic, NO macros — names are first-class strings (TOML keys, CLI flags), and plain fns keep grep + jump-to-definition clean. Implementation confined to `src/theme/mod.rs` only (single file diff; +204/-1 LOC at commit 107c917, including 8 new tests).
- 05-02: `Palette::default()` now DELEGATES to `Palette::notion_soft()` (single source of truth — every existing Phase 1–4 snapshot / visual test resolves to identical RGB triplets). Pinned by two tests: `default_equals_notion_soft` (direct equality) + `notion_soft_matches_legacy_default` (literal-RGB exactness on all 9 fields — guards against accidental tuning that would silently break every prior snapshot).
- 05-02: PRESET PALETTE — cyberpunk_neon: bg `#0A0612` (near-black, purple-bias), edge `#4A366E`, glow `#FF2D95` (hot magenta), volume `#00E5E5` (electric cyan), running `#C0FF33` (electric lime), paused `#FFC800`, stopped `#554A68`, restarting `#00B4FF`, crashed `#FF3A14` (hazard red-orange). High-contrast hacker-thriller look; status hues are deliberately pulled apart so the lifecycle reads at any size.
- 05-02: PRESET PALETTE — terminal_green: bg `#000A00` (CRT black), edge `#408040`, glow `#80FFA0` (bright phosphor), volume `#40A040`, running `#33FF33`, paused `#C8C840`, stopped `#406040`, restarting `#80C880`, crashed `#FF4040`. Monochrome phosphor green family (VT100 / IBM 3270 look) EXCEPT crashed deliberately breaks to red — without that break, crashed disappears into stopped at small sizes. The intentional break is documented in the constructor doc-comment.
- 05-02: DISPATCHER TOLERANT TO TOML / CLI CASE — `Palette::by_name(name: &str)` does `name.replace('_', "-").as_str()` BEFORE the match, so `cyberpunk_neon` and `cyberpunk-neon` both resolve to the same preset (pinned by `by_name_accepts_snake_and_kebab_case`). Unknown name returns `None` (caller chooses log/error/default policy). Companion `by_name_or_default(name) -> Self` never returns None — falls back to notion_soft — so main.rs can honor `AppConfig.palette` without ever crashing on a typo'd key.
- 05-02: TRUECOLOR INVARIANT pinned by `every_preset_has_all_fields_as_rgb` — pattern-match every field of every preset against `Color::Rgb(_,_,_)`. Rationale: the renderer's fog / dim math passes non-RGB colors through unchanged, so a preset slipping in `Color::Reset` or a 16-color named variant would silently break depth shading. 05-05's quantization pass will rely on this invariant — if a future preset adds a non-Rgb field this test catches it first.
- 05-02: VISUAL DISTINGUISHABILITY pinned by two tests (`three_presets_have_distinct_running_colors`, `three_presets_have_distinct_glow_colors`) — running + glow are the two dominant signals; if a future tuning collapses any pair the test fails. Volume / paused / stopped / restarting / crashed are intentionally NOT pinned distinct across presets (e.g. notion_soft and cyberpunk_neon could converge on a paused amber and that's fine).
- 05-02: THEME-01 INVARIANT INTACT — `grep -rn 'Color::Rgb' src/ | grep -v 'src/theme/'` set DID NOT GROW (only pre-existing 04-03 / 04-04 / 04-05 test-fixture + scale_brightness destructuring hits remain). Recommended 05-04 / 05-05 verify: re-run that grep, confirm count unchanged. The single allowed locus for new preset literals is `src/theme/mod.rs`.
- 05-02: 1 corrective atomic commit — `107c917` (Task 1 three presets + by_name + 8 tests). OnixDebian author; no Claude mentions; conventional-commit format with `(05-02)` scope. Tests 277 → 285 (+8 in theme::). cargo build --release + cargo test all clean. NB on history: an EARLIER commit `88791ef` (also titled `feat(05-02)`) accidentally captured 05-01's `src/config/mod.rs` (auto-watcher staging-area reshuffle between Read and `git add`); `107c917` is the corrective real-05-02 landing. End-state on disk is fully consistent; the only artifact is the misleading title on `88791ef`. 05-01's main.rs wiring landed in `7758aa6`, which cleared the dead-code clippy errors that had been blocking `--all-targets -D warnings`. Re-verified post-107c917: `grep -rn 'use bollard' src/ | grep -v src/docker/` returns nothing (bollard isolation intact); `grep -rn 'Color::Rgb' src/ | grep -v 'src/theme/'` set unchanged from pre-05-02 (THEME-01 invariant intact).

**Phase 2 (Scene Pipeline):**
- 02-01: LAYOUT RESOLVED (rack-grid vs network-floors) → network-grouped rack grid. Groups = contiguous Z-bands (Phase 4 ENT-01 floor-planes drop in as the band's plane); within a group boxes fill columns/X and shelves/Y. Network grouping is the first-class placement axis.
- 02-01: `src/world` is pure deterministic data — layout()/sizing are closed-form functions of stable id/group (NO clock, NO rand, NO global mutable state). synthetic_scene() = 50 boxes / 5 groups, deterministic per-id load (Knuth hash) + status variety (id%10).
- 02-01: Sizing constants — MIN_HALF 0.3, MAX_HALF 1.2; load_to_half_extent is sqrt-compressive, clamped, NaN-safe (CONT-02). Layout consts — SLOT_SPACING 3.0 (>2*MAX_HALF, no-overlap), GROUP_DEPTH 18.0 (>intra-group spread, clustering), GRID_COLS 4.
- 02-01: Entity.status REUSES theme::Status (CONT-01); world carries ZERO inline RGB (THEME-01 holds) — color resolved downstream by Palette.
- 02-01: Camera::frame_scene targets bounds.center, radius = scene_r*(1 + 1/tan(theta)) — NEAR-CORNER tangent bound against the cell-aspect-narrowed HORIZONTAL half-FOV (FRAME_HALF_FOV 0.367, FRAME_SAFETY_MARGIN 0.15), NOT the vertical sphere bound. Pinned frustum-safe across full yaw orbit by frame_scene_keeps_whole_scene_in_frustum.
- 02-01: RenderConfig.far 100->500 (blocking fix) — the deep rack + pulled-back framing radius put far corners beyond the old far plane; single-cube fog is absolute so unaffected.

**Post-verification rendering evolution (2026-05-27, human-driven, all under 01-05):**
- ARCHITECTURE PIVOT → DUAL RENDER BACKEND, auto-selected by terminal capability (`src/kitty.rs::supports_kitty_graphics`, dispatched in `main.rs`):
  - **kitty graphics protocol (real RGB pixels)** in kitty/ghostty/wezterm — smooth, anti-aliased, no braille staircase ("ratty-quality"). PREFERRED high-quality path.
  - **braille** fallback everywhere else (Alacritty, SSH, dumb terminals — Alacritty has NO graphics protocol at all, unfixable there).
  - Override flags `--kitty` / `--braille`; `--dump-rgba <path>` writes one RGBA frame for offline inspection (how the kitty render is verified without a capturable display).
- kitty renderer (`src/kitty.rs`): per-pixel z-buffer (exact occlusion — well-suited to Phase 2's many overlapping boxes), 2× supersampled AA, square pixels (cell_aspect 1.0), flat per-face shading, zlib-compressed frames (`o=z`, ~148× smaller payload via `miniz_oxide`), status bar on reserved bottom row, radius 3.2. MUST build `--release` (debug raster ~79ms/frame ≈12fps; release ~11ms).
- braille shading refined: faces stay FLAT — resolve picks the DOMINANT face color per dot (mode), never a cross-face blend (blend darkened wall-tops); majority-coverage threshold drops faint AA "dribble" specks. Edge-outline experiment was tried and REVERTED (faces looked uneven / flickered).
- fog made ABSOLUTE (camera-distance ± cube bound), not per-frame visible-face min/max — fixed top-face brightness flicker.
- INPUT RESPONSIVENESS FIX (real bug): unbounded event channel + heavy render → Render/Tick backlog buried quit keys → app sometimes wouldn't quit on q/Esc/Ctrl-C. Fixed via `MissedTickBehavior::Skip` + main loop drains the queue and COALESCES Render to one draw (`tui::try_next`, `app::run`).
- Tests now 36 (added kitty `top_face_shade_is_yaw_invariant`).

### Pending Todos

- Phase 4 plan 06b (detail panel — VISIBLE POPUP + off-thread spawn + final phase verify) remaining. 04-06a landed the bollard-free data substrate (StatSample.blkio_r/w, fetch_detail, DetailSnapshot, DockerMsg::Inspected, LiveWorld cache, Selection.pending_detail + inspect_in_flight). 04-06b interprets Effect::SpawnInspect from 04-03's dispatch (tokio::spawn fetch_detail; feed Inspected back through the mpsc), populates Selection.pending_detail from LiveWorld::last_inspect, renders a ratatui Clear+Block popup in App.update (braille) + a centered cell-block overlay in run_kitty (kitty), and closes the Phase 4 finale with a self-verified visual checkpoint against a real daemon.
- ~~Phase 4 plan 05 (volume cylinders + image stacks)~~ RESOLVED — ENT-03 + ENT-04 shipped via the extended SceneExtras (W7 closure intact); Pitfalls F + G + H all closed; mount-count proxy for cylinder height; BTreeMap-id-ordered image stacks at deterministic MAX_RACK_X + offset (W9 closure).
- ~~Phase 3 carryover (1): exited-with-OOM seed mis-mapped to Stopped~~ RESOLVED in 04-02 via `enrich_snapshot_on_seed` + `status_override: Some(Crashed)` gate in `handle_enriched`.
- ~~Phase 3 carryover (2): `create` events seed `group_key: "none"`~~ RESOLVED in 04-02 via `enrich_snapshot_on_start` (off-thread inspect spawn in the `start` event arm; LiveWorld migrates the slot via `handle_enriched`).
- ~~Phase 3 carryover (3): no slot migration when a container's network changes mid-life~~ RESOLVED in 04-02 via `LiveWorld::handle_enriched`'s slot-migration path (free old slot like Removed, allocate lowest-free in new group; anti-teleport preserved for non-migrating neighbors).
- For Phase 4 planning: `events()` subscribes without filters; client-side filtering on `EventMessageTypeEnum::CONTAINER`. Switch to server-side `filters({"type": ["container"]})` if wire chatter becomes a problem.
- For Phase 5 (carried from 04-05): image event subscription deferred. `docker pull` doesn't reflect until app restart. A small `streams.rs` extension can subscribe to `type=image` events and feed `DockerMsg::ImageAdded`/`ImageRemoved` so the stack region updates live.
- For Phase 5 (carried from 04-05): volume bytes-on-disk DATA-01 deferred. Mount-count proxy is correctness-lossy. v2 on-demand `system df -v` scan behind an explicit user key.
- For Phase 5 (carried from 04-05): image stack labels (repo_tag) deferred. `image_stack_positions()` already returns the tag; the v1 backends drop it. Phase 5 can add a label tier for selected stacks.
- For Phase 5 (carried from 04-05): `MAX_VISIBLE_IMAGE_STACKS=12` is a magic number. Phase 5's theming/config layer can lift it to a runtime preference.
- For Phase 5 ROB-02 (carried from 03-04): mid-session daemon-loss UX currently "freezes on last World" (no panic, no void — hard requirement met). Phase 5 should add a "daemon lost" in-scene banner + reconnect attempt. The probe-before-TUI pattern from 03-04 is the template for any capability-degrade gate.
- For Phase 5 (carried from 03-04): `--dump-rgba` stays daemon-free by design (renderer inspection tool). If a live-data offline dump is ever needed, build a small separate tool that seeds a `LiveWorld` from one-shot `list_containers` and dumps rgba.
- Pre-existing fmt drift on the pre-phase-3 files (`src/world/scene.rs`, `src/app.rs`, etc.) — NOT touched by this plan; pick up in a separate `chore(fmt)` whenever.

### Blockers/Concerns

- ~~Legibility (will it look good?)~~ RESOLVED — Phase 1 legibility spike APPROVED by human in a real terminal; cube reads as a solid 3D form
- ~~01-01 interactive verification UNVERIFIED~~ RESOLVED — q/Esc restore, resize-no-garbage, panic restore all confirmed working in a real terminal during the 01-05 verify
- ~~Layout algorithm (rack-grid vs network-floors) unresolved~~ RESOLVED in 02-01 — network-grouped rack grid (groups as Z-bands)
- Inter-box draw ordering: braille path uses painter's sort (fine for convex boxes, watch overlapping boxes across groups); kitty path's per-pixel z-buffer already handles arbitrary overlap
- ~~Volume size not exposed by Docker API — decide proxy metric in Phase 4 planning~~ RESOLVED in 04-05 — mount-count proxy via `world::volume::proxy_volume_height(mount_count)` sqrt-compressive (Pitfalls F + G closed). v2 DATA-01 can layer a real `system df -v` scan behind a user key.

## Session Continuity

Last session: 2026-05-30
Stopped at: 05-02 COMPLETE (Phase 5 of 5 IN PROGRESS — three named palette presets + by_name dispatcher landed cleanly at commit 107c917). cargo build --release + cargo test 285/285 all clean. THEME-02 functionally satisfied; runtime switching lands in 05-04.
Resume file: None
Resume note: Phase 5 plan 02 done — named palette presets. Single corrective atomic commit by OnixDebian, no Claude mentions:
  - 107c917: feat(05-02): three named palette presets + by_name dispatcher (+204/-1 LOC in src/theme/mod.rs; 8 new tests)
Tests: 285/285 (+8 new in theme::). SUMMARY at .planning/phases/05-theming-config/05-02-SUMMARY.md. KEY PIECES NOW READY FOR 05-03 / 05-04:
  - `crate::theme::Palette::notion_soft()` / `cyberpunk_neon()` / `terminal_green()` — three preset constructors.
  - `crate::theme::Palette::by_name(name: &str) -> Option<Self>` — kebab + snake-case tolerant string dispatcher.
  - `crate::theme::Palette::by_name_or_default(name: &str) -> Self` — typo-safe fallback (returns notion_soft on unknown).
  - `crate::theme::Palette::default()` STILL == `Palette::notion_soft()` (delegation single-line; zero-regression for every Phase 1–4 snapshot).
HISTORY NOTE: An earlier commit `88791ef` (also titled `feat(05-02)`) accidentally captured 05-01's `src/config/mod.rs` due to an auto-watcher staging-area reshuffle between Read and `git add`. `107c917` is the corrective real-05-02 landing; on-disk state is fully consistent. The 05-01 work itself is also complete (`373a923` deps + 88791ef's diff content + `7758aa6` main.rs wiring).
THEME-01 INVARIANT INTACT: `grep -rn 'Color::Rgb' src/ | grep -v 'src/theme/'` set DID NOT GROW; only allowed locus for new preset literals is `src/theme/mod.rs`.
NEXT: 05-03 (omarchy-derived 4th palette; depends only on 05-02's dispatcher — can land before 05-04 since 05-04 will then wire ALL FOUR through `AppConfig.palette`) OR 05-04 (runtime palette switching — THEME-04, depends on both 05-01 AppConfig.palette and 05-02's presets; 05-04 also removes the `let _ = config;` stub in main.rs). The natural next plan is 05-03 (smaller scope, then 05-04 inherits both).
